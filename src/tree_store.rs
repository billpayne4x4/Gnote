// pub struct TreeStoreManager;

// impl TreeStoreManager {
//     pub fn add_folder(store: &gtk::TreeStore, name: &str) {
//         store.insert_with_values(None, None, &[(0, &&name), (1, &&"folder"), (2, &&"Data")]);

//     }
// }
